language: node_js

cache:
  directories:
  - ~/.npm

node_js:
- '8'

before_install:
- npm i -g npm@5.7.0

install:
- npm ci

script:
- npm run cloc
- npx jscpd
- npm run tslint
- npx tsc

after_success:
- npm run travis-deploy-once "npm run semantic-release"
